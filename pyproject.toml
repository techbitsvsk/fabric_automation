[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
# Add project root to sys.path so `import app` resolves without installation
pythonpath = ["."]
testpaths  = ["tests"]